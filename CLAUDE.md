# 한글 워들 클론 코드 리뷰

## 목적

학습용 프로젝트. 동작보다 상태 설계와 로직 구조에 집중해서 리뷰해줘.

## 스택

- React + Vite + TypeScript
- Tailwind CSS
- React Router (Phase 2.5부터)

## 리뷰 기준

- 게임 상태 구조가 적절한지
- 자모 분리 및 판정 로직이 올바르게 설계됐는지
- 컴포넌트 분리가 자연스러운지
- TypeScript 타입 설계가 적절한지

## 향후 리팩토링 계획

- Socket.io 실시간 멀티플레이 예정
- 현재 싱글 플레이 + 링크 공유 플레이 구조는 의도된 것

## 절대 하지 말아야 할 것

- 코드를 직접 수정하거나 고쳐서 보여주는 것
- 이유 설명 없이 고친 코드만 보여주는 것
- "이렇게 바꾸세요" 같은 완성 코드 제시
- 문제점 나열 없이 바로 해결책부터 말하는 것
- 잘 된 부분 언급 없이 문제점만 지적하는 것

## 기능 명세

### 1단계 · 싱글 플레이

- 자모 5개짜리 단어를 5번 안에 맞춘다
- 단어 입력 시 자모로 분리되어 판정 (초록/노랑/회색)
- 중복 자모는 정답에 있는 개수만큼만 노랑 처리
- 화면 키보드에 판정 결과가 반영된다
- 자모 합계가 5개가 아닌 단어는 제출 불가
- 성공 시 결과 페이지로 이동, 실패 시 정답 공개
- 결과 페이지에서 소요 시간 + 진행 기록 표시
- 결과 이모지 클립보드 복사 (제시어 제외)

### 2단계 · 링크 공유

- 홈에서 초대 링크 생성 및 복사
- 링크 진입 시 동일 제시어로 게임 시작
- 만료된 링크 차단 + 안내 메시지
- 같은 링크 재진입 시 결과 페이지로 바로 이동

### 자모 분리 규칙

- 초성·중성·종성 모두 개별 자모로 취급
- 복합 모음(ㅘ, ㅚ 등) 미사용
- 자모 분리 라이브러리 미사용, 유니코드 수식 직접 구현
